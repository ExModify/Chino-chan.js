module.exports = {
    name: 'waifu',
    canPrivate: true,
    requirePrefix: true,
    minimumLevel: 0,
    type: "Fun",
    execute: (bot, message, prefix, command, parameter, language) => {
        message.channel.send("no.");
    }
};