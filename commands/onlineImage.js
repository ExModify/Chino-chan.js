module.exports = {
    name: 'onlineImage',
    canPrivate: true,
    requirePrefix: true,
    aliases: [],
    execute: (bot, message, prefix, command, parameter, language) => {
        let parameters = parameter.split(' ');
        parameters[0] = parameters[0].toLowerCase();
        if(parameters[0] == "sankaku"){

        }
        else if (parameters[0] == "gelbooru"){

        }
        else if (parameters[0] == "pixiv"){

        }
        else{
            
        }

    }
};